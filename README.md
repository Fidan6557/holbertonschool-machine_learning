# holbertonschool-machine_learning
Description

This task implements a function that converts a NumPy ndarray into a pandas DataFrame.

The resulting DataFrame must:

Have columns labeled with capitalized letters in alphabetical order (A, B, C, ...).

Support up to 26 columns (A–Z).
